#include<stdio.h>

int main()
{
   
    /* 
    question 1:
    a)
    30
    25
    20
    15
    10
    5 
    
    b)
    75
    65
    55
    45
    35
    25
    15

    c) question wrong

    d)
    10
    30
    50
    70
    90

    question 2:

    a) for (int i = 4; i < 29; i+=4)
       {
        printf("%d,", i);
       }

    b)  for (int i = -3; i > -19; i-=3)
       {
        printf("%d,", i);
       }  

    c)  for (int i = 1; i <8; i++)
       {
        int k= i*i;
        printf("%d,", k);
       }

    d)  for (int i = 10; i> -3; i-=2)
       {
        printf("%d,", i);
       }

    question 3:

    a) correct code: 
        int i=0;
        do
        {
        printf("%d,",i);
        i++;
        } while ( i<5);

    b) correct code:
        for (int i = 0; i < 5; i++)
         {
        if (i%2 == 0)
         {
         printf("%d is even\n",i);
          }
        else
        {
        printf("%d is odd\n",i);
          }
        }

    c) correct code:
        int grade = 85;
        switch (grade)
        {
        case 90 ... 100 :
            printf("A\n");
            break;
        case 80 ... 89 :
            printf("B\n");
            break;    
        
        default:
            printf("C or below \n");
            break;
        }

    d) correct code:
        int sum=0;
        for (int i = 0; i <= 5; i++)
        {
            sum+=i;
        }
        printf("Sum is : %d\n",sum);

           

    question 4:
        int e_Id,state;
        double hourly_price,wwork_time,salary; //weekly work time double:)

        printf("Enter your employee id:");
        scanf("%d",&e_Id);
        printf("Enter your hourly price:");
        scanf("%lf",&hourly_price);
        printf("Enter your weekly work time:");
        scanf("%lf",&wwork_time);

        
        if (wwork_time<=40 && wwork_time>=0)
        {
            state=1;
        }else if (wwork_time>40)
        {
            state=2;
        }
        
        
        switch (state)
        {
        case 1:
            
                salary=hourly_price*wwork_time;
                printf("%d id numbers employee salary: %.3lf",e_Id,salary);
                break;
        case 2:
            
                salary=(40*hourly_price)+(1.5*hourly_price*(wwork_time-40));
                printf("%d id numbers employee salary: %.3lf",e_Id,salary);
                break;
        
        default:
            break;
        }
   
   */

    return 0;
}
